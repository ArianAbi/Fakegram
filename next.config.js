/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['jjpewexacillcatitbra.supabase.co']
    },
    experimental: {
        serverActions: true
    }
}

module.exports = nextConfig
